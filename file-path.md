# Concepts
- `.` - current directory
- `..` - parent directory

# Normalize a path (make absolute and remove redundant segments)
```bash
# bash
realpath $path
```

- `basename`
- `dirname`
- `readlink`
